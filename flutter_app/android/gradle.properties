org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# MapLibre 0.27+ requires JDK 21 for Android builds; point Gradle to the
# installed JDK 22 so the build succeeds even when Android Studio's bundled
# JBR is 17. The path is local-only and not committed to CI.
org.gradle.java.home=F\:\\Program Files\\Java\\jdk-22
